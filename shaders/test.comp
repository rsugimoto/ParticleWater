#version 430

layout(local_size_x=16) in;

layout(std430) buffer shader_data{
    vec4 array[];
};

void main(){
    uint threadIndex = gl_GlobalInvocationID.x;
    array[threadIndex] = vec4(float(threadIndex));
}
